/// <summary>
/// Central place to bump the injector's version. Update <see cref="Current"/> whenever
/// injector behavior changes so output maps can be traced back to the build that produced them.
/// </summary>
public static class AppVersion
{
    public const string Current = "1";
}
